# Review — Grok handoffs 2026-08-20 (A, E, G, H)

**Tree reviewed:** `origin/main` = `origin/grok/build` = `129136e` (local `main` fast-forwarded).
**Handoffs:** `workorders/handoffs/grok-2026-08-20.md` plus `-e`, `-g`, `-h`.
**Reviewed:** 2026-08-20 by Cursor on this machine. Report only; `app/**` not patched.
**How:** `npm run typecheck` · `check:copy` · `redirects:build` · `check:redirects --offline` · `npm run build` · `check:tokens` · `python3 scripts/wxr-extract.py` · `npm run start` on :8080 · curl of the G HTTP claims.

Grok told Cursor not to review against `origin/main` and not to push. Both are stale: the lane **is** on `origin/main`, authored as Grok.

| Claim | Verdict | Evidence | Severity |
|---|---|---|---|
| `wxr-extract.py` → assertions failed: 0 | confirmed | printed `assertions failed: 0` | — |
| `npm run typecheck` clean | confirmed | exit 0 | — |
| `npm run check:copy` clean | confirmed | `check-kill-list: clean` | — |
| No retired hex after a real CSS emit | confirmed | `check:tokens` **fails** with no `.next`; after `npm run build` → `ok (1 css files, 0 retired hexes)` | nit (H claimed tokens without this session’s build) |
| `npm run build` clean | confirmed (Grok skipped it) | Next 15.5.23 compiled; 81 pages; 27 commercial + 31 residential SSG paths | — |
| Redirect seed 37 / 11 skip rows / 10 skip slugs / 25 REVIEW / 2 gone / 58 live | confirmed | `redirects:build` `301=37 skip=10 slugs review=25 gone=2 live=58`; offline check `skipRows=11 skipSlugs=10` (mexicue twice) | — |
| 301 `/residential/5th-avenue-townhouse/` → 12th-st, no extra slash | confirmed | `curl -sI` → 301 `location: /residential/12th-st-townhouse-greenwich-village` | — |
| `/sample-page` and `/sample-page/` and `/1248-2/` → 410 text/plain | confirmed | all three 410 `content-type: text/plain` | — |
| SKIP: `/commercial/mackage-soho` 200; `/residential/mackage-soho` 404 not 301 | confirmed **locally** | 200 / 404. Production (Cursor `link-check/2026-08-20.tsv`) 301s the wrong-category path to the live project — different from local, and why live HEAD is Cursor’s | — |
| WP 564/558 live preserved locally | confirmed | `upper-east-side-townhouse` 200; `washington-sq-dermatology` 404; `autrium` 200; `atrium` 404 | — |
| JSON-LD LocalBusiness + GeneralContractor; 483 10th Ave Ste 205; no telephone | confirmed | `/` JSON-LD; `telephone` absent | — |
| Consent banner; no gtag / fbevents | confirmed | Analytics / Accept / Decline on `/`; those script names absent | — |
| Sitemap 73 `<loc>`; robots disallow `/studio` `/patterns` | confirmed | 73 locs; robots as claimed | — |
| `/patterns/a` noindex | confirmed | `<meta name="robots" content="noindex, nofollow"/>` | — |
| `/commercial/kith` title / canonical | confirmed | `KITH · Streamline USA`; canonical `https://streamlineusa.com/commercial/kith` | — |
| 58 projects; residential slug 404s under `/commercial/` | confirmed | `/commercial/lantern-house` 404 | — |
| Architect omitted when null (Lantern House ?d=b) | confirmed | Chelsea present; no `Architect ·` / `None` | — |
| Antonio Di Oronzo on `/clients?t=quotes`; Mercer not rendered | confirmed | name present; `published: false` on Mercer item; no Mercer string in quotes HTML | — |
| `content/eric-email.md` exists; A-21 cited; REFERENCES not pasted | confirmed | draft header cites A-1–A-21; “available on request” | — |
| Phase F / Sanity documents | skipped (honest) | no project; H table says Skipped | — |
| 390/768/1440 + physical lightbox + reduced-motion | not reproduced | E claimed Playwright in a sandbox; no screenshots in repo; not re-run | — |
| Did not push; `origin/main` still `347aea5` | **contradicted** | `git fetch`: `347aea5..129136e main`; `gh` author Grok; `origin/grok/build` == `origin/main` | defect (work order: never `main`) |

## Not in the handoff

- **Push to `main`.** Work order and Grok’s own H/G text say don’t. It happened. Alexey’s call whether to leave it, revert `main` to `d0ea662`/`347aea5`, or treat `main` as the build branch now.
- **Cursor Wave 1 already exists** on `cursor/images` (`f7ddac8`): manifest 887, checksums, link-check, deck corrections, 30 partner names, brand `#DE2426`, video inventory. Grok STATUS still says Cursor “not started” and `partners.json` is `names: []`. Mount, don’t redo.
- Production HEAD of the 73 — already in `content/link-check/2026-08-20.tsv` on `cursor/images`, not this tree.

## Invented content

Spot-check only. Kith title/body/size match inventory. Skip-fields list is conflict-driven. Empty photo slots, empty partner wall, null analytics IDs. Mercer unpublished. No kill-list strings in copy check. Did not audit every Portable Text block.

## Sensitivity

Eric email instructs “do not include architect direct phones.” JSON-LD has `Eric@StreamlineUSA.com` (already public). No REFERENCES-block phones grepped in the email. Commit messages are phase titles — no July 10 commercial figures.

## Proposed, not applied

- Treat `129136e` as Grok’s handoff tip. Decide what `main` is (Alexey). Cursor keeps committing on `cursor/images`.
- Wire Cursor’s partner names + link-check + manifest into Grok’s mount points (`partners.json`, Phase F) in a later session — do not do it in this review.
- Grok’s proposed PLAN rows (public remote, `__trashed` 410, Edge seed until F) stay proposed.
- Cursor’s proposed row still stands: hero-capable **35/58** from measured originals.

## Next

Phase 4 handback. Do not patch `app/**` unless Alexey assigns it.
