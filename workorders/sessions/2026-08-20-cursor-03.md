# cursor session 03 — 2026-08-20

**Phase:** Track C review of Grok A/E/G/H on `main` @ `129136e`
**Branch:** review written for `cursor/images`; inspected tree is `main`
**Commits:** review + this log (this session)

## Did

- User said Grok pushed to `main`. `git fetch`: `origin/main` and `origin/grok/build` both `129136e`.
- Fast-forwarded local `main` (`d0ea662` → `129136e`). Did not merge that into `cursor/images`.
- Reproduced cheap claimed gates (typecheck, copy, redirects, wxr, **build**, tokens-after-build, local HTTP on `npm run start :8080`).
- Wrote `workorders/reviews/grok-2026-08-20.md`. Did not patch `app/**`.

## Landed

| Path | What |
|---|---|
| `workorders/reviews/grok-2026-08-20.md` | confirmed / contradicted / skipped |

## Numbers

- Build: 81 pages; 27C + 31R project paths.
- Redirects: 37 301, 11 SKIP rows / 10 slugs, 25 REVIEW, 2×410, 58 live.
- `assertions failed: 0`.

## Decisions I made on my own

- Reviewed against `main` because that is where Grok landed, despite the handoff saying not to.
- Cursor artifacts stay on `cursor/images`; this review will be committed there.

## Proposed, not applied

See the review file. Hero 35/58 still proposed.

## Blocked

- Phase F: Eric + Sanity + merge of Cursor image artifacts onto the build tree.
- `main` vs lane policy: Grok pushed to Alexey’s branch.

## Skipped

- Viewport / physical lightbox / reduced-motion
- Patching Grok files
- Merging `cursor/images` into `main`

## Next step

Claude Code: do not start Sanity or alt-text. Decide `main` vs `cursor/images`. Send the Eric email from the `main` tree.

## Claude Code handback

Two git tips, not one tree.

| Tip | SHA | Holds |
|---|---|---|
| `cursor/images` | this branch | image manifest, checksums, link-check, deck corrections, partners, brand, videos inventory, this review |
| `origin/main` = `origin/grok/build` | `129136e` | Next app, schema, copy, eric-email, redirects, directions A/B/C |

1. **`main` policy** — Grok pushed the build onto Alexey’s branch. Either leave it, move the build back to `grok/build` only, or revert `main`. Cursor should keep committing on `cursor/images`.
2. **Do not merge blindly.** Both sides edited `STATUS.md` / `scripts/README.md`. Mount Cursor files into Grok’s paths (`partners.json` still `names: []` on `main`).
3. **Eric email** — `content/eric-email.md` on `main`. Alexey sends. Not sent.
4. **Videos** — two WhatsApp reels outside the repo; end logos on; variants still unbuildable. Questions in `content/images/video-ask.md` on `cursor/images`.
5. **Sanity / Phase F** — later. Needs Eric answers + project + the 887 manifest from `cursor/images`.
6. **887 alt-text** — still deferred.
7. **Do not push `cursor/images`** unless Alexey says so. `main` is already on the public remote.
8. **Proposed PLAN:** hero 35/58 (Cursor); Grok’s public-remote / `__trashed` 410 / Edge-seed rows. Alexey applies.
