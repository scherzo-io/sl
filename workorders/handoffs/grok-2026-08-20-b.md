# Handoff — Grok Phase B — 2026-08-20

Cursor reviews this. Reproduce the claims; don’t take them on faith.

**Branch:** `grok/build` (not pushed — remote is public)
**Phase:** B scaffold

## Claims (reproducible)

| Claim | Command |
|---|---|
| Extractor still green | `python3 scripts/wxr-extract.py` → `assertions failed: 0` |
| Typecheck | `npm run typecheck` |
| Production build | `rm -rf .next && npm run build` — 18 routes, no type errors. Do not run this while `next dev` is using the same `.next`. |
| No retired hexes in built CSS | `node scripts/check-retired-hex.mjs` after a build. Retired set: `#D42E12` `#E8492B` `#E85A3C` `#FF0000` `#990000` `#808285`. Observed hexes: `#000` `#fff` `#1a1a1a` `#2d2d2d` `#da2128` `#e25257`. |
| Patterns at 390 / 768 / 1440 | `node scripts/qa-patterns.mjs http://127.0.0.1:8080` (needs Playwright on the machine). Routes: `/` (A), `/portfolio` (B), `/services` (C). |

## Verified here (sandbox Chromium)

- `/` `/portfolio` `/services` return 200. No page errors, no console errors, no horizontal overflow at 390 / 768 / 1440.
- Desktop (≥1024): 200px sidebar, red wordmark, sentence-case nav, email, © current year + Streamline USA LLC. No phone, no social icons.
- 768 and 390: dark top bar, hamburger, single column.
- Pattern B: one Filter control (All / Commercial / Residential); 58 empty tiles at native `featured_dims` aspect; no captions.
- Pattern C: white column + empty photograph plane; copy centred; 16px body.

## Unverified (your screen / keyboard)

- Contrast of `#DA2128` / `#E25257` on real pixels (computed in DESIGN.md; not measured here).
- BenchNine 300 actually rendering as 300, not a faux bold.
- Touch targets on the hamburger / Filter at 390.
- Lightbox — not built (Phase E).

## Skips / empty on purpose

- `siteSettings.phone` — not rendered.
- Social — none in the export chrome.
- Photographs — empty `ProjectImageSlot`, no stock, no generated pixels, no WP CDN.
- `/studio` — title only; no Studio mount until a project id.
- Partner bios — name + title only (deck bios carry staffing/revenue/336 W 37th).
- `/clients` and `/videos` and `/request-for-pricing` — title only.
- Procore sentence, about.txt lorem, `desiogn`, Mercer testimonial.

## Files close to the ownership line

- Read `content/image-audit.tsv` (Alexey’s) for tile aspects. Did not edit it.
- Did not touch `content/images/**` or `inputs/raw/brand/streamline-logo.png`.
- Favicon is a geometric SVG in `public/favicon.svg`, not a logo trace.

## How to run

```bash
cd ~/sl
git checkout grok/build   # once Alexey has the commits; until then, this machine
npm install
npm run dev               # 0.0.0.0:8080 in this sandbox; locally whatever Next prints
```
