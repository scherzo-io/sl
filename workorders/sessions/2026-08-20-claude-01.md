# claude session 01 — 2026-08-20

**Phase:** review only — both lanes against their work orders (`handoff-to-claude.md`)
**Branch:** `main` @ `b2b1fc1` (review committed on top; nothing pushed)
**Commits:** see below

## Did

- Verified the tree state first, and found the handoff's premise stale: `origin/main` is
  `b2b1fc1`, identical to local `main`. Nothing is 9 commits ahead; everything is already on the
  public remote.
- Ran every runnable gate: `wxr-extract` (0 failures), `typecheck`, `check:copy`,
  `redirects:build`, `check:redirects --offline`, `build` (exit 0), `check:tokens` (0 retired
  hexes).
- Verified artifact counts directly: manifest 887, checksums 887, partner names 30 (all 30
  rendering), `partners.json` `artwork: []`, 137 `parent_is_zero`, 0 rows missing `source_path`.
- **Reproduced Phase E on a real browser and keyboard** — the gate Cursor could not reach. 200px
  sidebar, non-scrolling landing, 390 collapse, lightbox arrows/Escape/focus-trap, focus ring
  measured at `rgb(218,33,40)`.
- Joined Cursor's production link-check to Grok's redirect table, which is where the blocker came
  from.
- Swept invented content (fixed-string, not regex — the earlier bracket pattern was silently a
  character class) and sensitivity across the diff and the last 14 commit messages.

## Landed

| Path | What |
|---|---|
| `workorders/reviews/claude-2026-08-20.md` | the review: 1 blocker, 6 defects, 3 nits, plus what was confirmed, what I could not verify, and §9's branch audit |
| `workorders/sessions/2026-08-20-claude-01.md` | this log |
| `workorders/STATUS.md` | Last updated / In flight / Next up / blocked |

## Numbers

All reproducible from this repo unless marked.

| Figure | Value | Command |
|---|---|---|
| Legacy rows resolving on production | **73 of 73** | `content/link-check/2026-08-20.tsv` (measured-from-live 2026-08-20) |
| Legacy rows that 404 on the new build | **32** (25 REVIEW + 7 cross-category SKIP) | `curl -sL` against `npm run start` |
| SKIP rows serving 200 directly | **4 of 11** (7 are 301→200) | same |
| Manifest / checksums rows | 887 / 887 | `wc -l` |
| `featured_only` rows | **7**, not 8 — one project's featured sits in another's gallery | manifest column |
| Hero-capable | 480 images / 35 projects | `hero-measure-finding.md` |
| Retired hexes in built CSS | 0 | `npm run check:tokens` |
| Sidebar width, measured | exactly `200px` | `getComputedStyle` in-browser |
| Focus ring | `rgb(218,33,40)` = `#DA2128` | in-browser |
| Substantive deck corrections unapplied | **1 of 28** (`PROGORE`) | fixed-string diff vs `content/copy` |

## Decisions I made on my own

- Scored Sanity-side "Done looks like" boxes as **skipped-with-reason** rather than failures — F
  never ran, so they are unfailable. Scored the data layer behind them separately.
- Did not re-hit `streamlineusa.com`. Cursor's dated snapshot is sufficient and the handoff says
  don't flood production.
- Did not re-run `build-image-manifest.mjs --uploads`; checking its outputs avoids dirtying
  `content/images/`.
- Investigated two apparent defects before filing them, and dropped both: the two "missing"
  partner names are `&amp;`-escaped, and `indeed` is `indeed-corporate-office-suite`. Also dropped
  a phantom "full-page scroll at 390" that was a 2× DPR screenshot.

## Proposed, not applied

Six PLAN §1 rows, listed in the review §6: measured targets for the 25 REVIEW rows; explicit 301s
for cross-category legacy paths; reworded SKIP gate; a trailing-slash decision; hero 35/58
(endorsing Cursor); public-remote remediation (endorsing Grok, with the correction that the push
already happened).

## Blocked

Nothing blocked this review. What it surfaced is owned by others: the redirect data decisions and
the trailing-slash call are Alexey's to assign; video masters and the private-repo flip are his
directly.

## Skipped

- `prefers-reduced-motion` under a real OS setting — verified the CSS rule and the JS hook instead.
- 768 px got a render check but not a full interaction pass.
- Did not patch anything, including the one-word `PROGORE` fix and the two stale `flags.json`
  entries. Both are one-line changes in Grok-owned files; the work order says report, and Alexey
  assigns.
- Did not re-review Grok phases A–D line by line against their handoff prose; scored them against
  the work order's gates and the disk.

## Addendum — branch audit (same session, after Alexey asked)

- `git fetch --all --prune`, then `git merge-base --is-ancestor <ref> main` for all six refs:
  every branch, local and remote, is an ancestor of `main`. `git diff --diff-filter=A` shows
  **zero** files `main` lacks. The only branch-side content is superseded `STATUS.md` prose
  (26 lines on `cursor/images`, 9 on `grok/build`). **Nothing is stranded.**
- Found a gap in my own first pass: **eight** Grok session logs exist, not seven — my inventory
  used `ls -R | head -40` and truncated at `grok-07`. Read `grok-08`; housekeeping only, no verdict
  changed. It does show Grok explicitly refusing to push a public client repo and proposing the
  private flip first — which strengthens §4 rather than changing it.
- Re-read `handoffs/grok-2026-08-20-h.md` line by line, which sharpened the blocker: the
  legacy-URL row's evidence cell says "25 REVIEW are intentional 404, not 410" while the row is
  scored **Passed locally**. The 404 was declared and mis-scored, not hidden.
- New finding (§9d): `sizeSqFt` "numeric on all 58" is unsatisfiable. `lib/skipFields.ts` suppresses
  the field on exactly the six projects with open size conflicts (A-1, A-5, A-10, A-11, A-12, A-14);
  verified in the expanded panel that non-conflicted projects render "4,500 sq ft" and the six
  render nothing at all. Correct behaviour, wrong gate — same class as G-2.
- Upgraded two H rows I could actually run: `build` (exit 0) and the `desiogn` typo (one hit, in
  `flags.json`, not rendered).
- Hygiene for Alexey: `origin/workorders/lane-scope-and-status` is fully merged and deletable;
  `origin/cursor/images` and `origin/grok/build` are both behind `main` now, so a lane resuming on
  its branch without fast-forwarding would work against a pre-merge tree.

## Next step

Alexey decides the redirect question — adopt the measured targets for the 25 REVIEW rows and add
301s for the 7 cross-category SKIP paths, or accept 32 URLs going to 404 at cutover. Everything
else in the review is a one-liner or a later phase.
