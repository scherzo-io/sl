# Handoff — Grok Phase G — 2026-08-20

Cursor: reproduce these claims. Do not take them on faith. Branch `grok/build` is not on origin (remote public — do not push). F was skipped on purpose; this phase does not need Sanity `redirect` documents.

## Claims

| # | Claim | Command |
|---|---|---|
| 1 | Typecheck clean | `cd sl && npm run typecheck` |
| 2 | Kill-list clean | `npm run check:copy` |
| 3 | No retired hex | `npm run check:tokens` |
| 4 | Extractor | `python3 scripts/wxr-extract.py` → `assertions failed: 0` |
| 5 | Redirect seed counts | `npm run redirects:build` → `301=37 skip=10 slugs review=25 gone=2 live=58` |
| 6 | Invariants + local HTTP | `npm run check:redirects` exit 0 |
| 7 | 301 one hop, trailing slash too | `curl -sI http://127.0.0.1:8080/residential/5th-avenue-townhouse/` → 301 `Location` ends `/residential/12th-st-townhouse-greenwich-village` (no extra slash) |
| 8 | 410 both slash forms | `/sample-page` and `/sample-page/` and `/1248-2/` → 410 `text/plain` `Gone` |
| 9 | SKIP live owner 200, wrong prefix 404 | `/commercial/mackage-soho` 200; `/residential/mackage-soho` 404 (not 301) |
| 10 | WP 564 / 558 live preserved | `/commercial/upper-east-side-townhouse` 200; `/commercial/washington-sq-dermatology` 404; `/commercial/autrium-corporate-office` 200; `/commercial/atrium-corporate-office` 404 |
| 11 | JSON-LD | `/` contains `LocalBusiness` + `GeneralContractor`, `483 10th Ave, Suite 205`, no `telephone` |
| 12 | Consent banner, no tags | `/` contains Analytics / Accept / Decline; no `gtag` / `fbevents` / LinkedIn pixel |
| 13 | Sitemap / robots | `/sitemap.xml` 73 `<loc>`; `/robots.txt` disallows `/studio` `/patterns` |
| 14 | noindex internals | `/patterns/a` and `/studio` have `noindex, nofollow` |
| 15 | Project metadata sourced | `/commercial/kith` title `KITH · Streamline USA`, description from PT, canonical `https://streamlineusa.com/commercial/kith` |

## Verified here

- 1–15 against `npm run dev` on 8080 and the JSON/TSV sources.
- 37 301s, 2 410s, 10 SKIP live owners, unique REVIEW paths: all HTTP-checked locally by `scripts/check-redirects.mjs`.

## Unverified (your machine)

- Live `HEAD` of the 73 on production. `content/link-check/` is not in this tree. Do not ask this sandbox to hammer SiteGround (captcha HTTP 202 last time).
- Contrast of the consent bar (`border-red` on `paper`) on a real screen.
- `npm run build` — not run; dev is on 8080.

## Skips

- Phase F / Sanity `redirect` documents. Seed is `content/copy/redirects.json`.
- Analytics IDs — `content/copy/analytics.json` all null. Banner is the visible cost; scripts are empty `if`s.
- WP 564 / 558 301s. Eric.
- REVIEW destinations. Alexey: `content/findings/legacy-review-rows.md` (25 rows, 13 slugs).
- Privacy policy page (none sourced).
- Hitting production.

## Files close to your boundary

None written under `content/images/**`. Did not create `content/link-check/`. Consume your live HEAD sweep when it lands; do not treat `legacy-slugs.tsv` `old_path` as a verified production URL (claimant category, not live category).
