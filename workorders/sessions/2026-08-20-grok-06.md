# grok session 06 — 2026-08-20

**Phase:** G (URLs, redirects, SEO) then H (verify and report). F skipped.
**Branch:** `grok/build`
**Commits:** this session (G then H)

## Did

- Seeded `content/copy/redirects.json` from `legacy-slugs.tsv` (37 301 / 11 SKIP / 25 REVIEW) plus PLAN §9 410s. Middleware looks that JSON up on the Edge (no `node:fs`).
- 410 `/sample-page` and `/1248-2` (with and without trailing slash). Next.js's default 308 slash-collapse ran first, so `skipTrailingSlashRedirect` is on and middleware owns it. 301s fire on the WordPress trailing-slash form in one hop. Live `/path/` 308s to `/path`.
- SKIP slugs never 301 in either category prefix. Live (category, slug) pairs 200. REVIEW 404. WP 564 and WP 558 live URLs unchanged.
- Metadata API: sourced descriptions where a body exists, omitted otherwise. Canonicals on `https://streamlineusa.com`. Patterns and `/studio` `noindex, nofollow`.
- JSON-LD `LocalBusiness` + `GeneralContractor`, 483 10th Ave Suite 205, NY 10018, `Eric@StreamlineUSA.com`. No telephone.
- Sitemap 15 pages + 58 projects. robots allow `/`, disallow `/studio` `/patterns`.
- Consent banner bottom-anchored, cookie `sl-consent`. Analytics IDs are all null — banner mounts, no scripts inject.
- Handed Alexey the 25 REVIEW rows as `content/findings/legacy-review-rows.md`.
- Phase H: ran the Grok-lane boxes in CLAUDE.md. Honest results in the H handoff.

## Landed

| Path | What |
|---|---|
| `scripts/build-redirects.mjs` `content/copy/redirects.json` | generated seed |
| `lib/redirects.ts` `middleware.ts` `next.config.mjs` | 301 / 410 / SKIP / slash |
| `lib/seo.ts` `app/*` metadata | titles, descriptions, canonicals, noindex |
| `components/seo/{JsonLd,ConsentBanner}.tsx` | JSON-LD + consent |
| `app/{sitemap,robots}.ts` | sitemap 73 URLs, robots |
| `content/copy/analytics.json` | IDs null |
| `content/findings/legacy-review-rows.md` | 25 REVIEW for Alexey |
| `scripts/check-redirects.mjs` | invariants + local HTTP |

## Numbers

- `python3 scripts/wxr-extract.py` → `assertions failed: 0`
- `npm run typecheck` clean. `npm run check:copy` clean. `npm run check:tokens` 0 retired hexes.
- `npm run check:redirects` → `301=37 skipRows=11 skipSlugs=10 review=25 gone=2 live=58 (27C/31R)` then HTTP clean against `http://127.0.0.1:8080`
- Sitemap `<loc>` count: 73 (15 pages + 27 commercial + 31 residential). No `/studio` or `/patterns` routes.
- Architect null at data layer: 8. Inventory `size_sqft_live` numeric on 58; 6 of those skipped at render (`lib/skipFields.ts`).
- Role raw maps to the six atoms on all 58. Unmapped: 0.

## Decisions I made on my own

- Redirect seed is JSON consumed by middleware, not Sanity `redirect` documents. F is skipped and there is no Sanity project. Studio description records the SKIP guard; it is not a runtime validation yet.
- REVIEW = 404, including `washington-sq-dermatology` and `atrium-corporate-office`. The `__trashed` artefact stays 404; the TSV recommends 410 — Alexey's call.
- Descriptions only on pages with their own sourced body. Portfolio / clients / videos / partners are title + canonical, no invented blurb.
- `skipTrailingSlashRedirect: true` so WP-style `/sample-page/` is 410 in one hop. Canonicals stay slashless (the app already shipped that way in E).
- Consent copy is three words: Analytics / Accept / Decline. No invented privacy policy.
- Next.js `permanent: true` is 308. 301s are middleware, not `next.config` redirects.

## Proposed, not applied

- PLAN §1: remote `scherzo-io/sl` is public — neither lane pushes. Still stands from session 01.
- PLAN §1 (new): `upper-east-side-penthouse__trashed` is never a public URL. Extractor says 410. Currently 404 with the other REVIEW rows.
- COWORK.md §6: `2026-08-20 | Grok G+H | 37×301, 11 SKIP still 200, 25 REVIEW 404, 2×410; consent banner with no IDs; F skipped; do not push`

## Blocked

- Phase F (Eric answers + Sanity project + Cursor images)
- Analytics IDs, Search Console, phones
- Live HEAD of the 73 on production — Cursor (`content/link-check/` absent)
- `npm run build` not run — dev is serving 8080

## Skipped

- Phase F
- Inventing GA4 / pixel / LinkedIn / Search Console IDs
- 301 for WP 564 / WP 558
- Partner names, photographs, video files
- Hitting production
- Sanity schema deploy / TypeGen (description-only change on `redirect.from`)

## Next step

Cursor reviews the G and H handoffs on a real screen. Alexey walks `content/findings/legacy-review-rows.md`. Do not start F.
