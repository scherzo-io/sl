# Handoff — Grok Phase C — 2026-08-20

**Branch:** `grok/build` (not pushed)
**Phase:** C schema

## Claims

| Claim | Command |
|---|---|
| Typecheck | `npm run typecheck` |
| Extract | `npm run schema:extract` writes `schema.json` |
| TypeGen | `npm run typegen` writes `sanity.types.ts` with no `any` |
| No deploy | `sanity schema deploy` was **not** run. `projectId` is the local placeholder `unconfigured`. |
| Named types | 11 documents (`project` `projectCategory` `page` `service` `person` `testimonial` `partner` `video` `siteSettings` `navigation` `redirect`) + `seo` + `blockContent`. |
| Role atoms | `lib/roles.ts` — six full names, not `GC`. |
| subCategory | `lib/subCategories.ts` — 10 workbook values, PLAN §1 row 14. No values assigned to projects. |

## Unverified

- Studio UI — not mounted.
- Schema against a real dataset — none exists.

## Skips

- Image fields have no assets (Cursor).
- `siteSettings.phone` empty.
- `video` documents: none, and youtube-id shape stands until row 32.

## Close to the ownership line

- Read `workbook-v2/filters.tsv` for the 10 names. Did not edit it.
