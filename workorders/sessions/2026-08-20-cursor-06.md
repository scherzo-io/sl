# cursor session 06 — 2026-08-20

**Phase:** bar C — alt, photography, video (findings first; alt paused mid first-wave)
**Branch:** `cursor/images` (fast-forwarded from `main` @ `789e06f`; local only)
**Commits:** `ff4a4fb` `2d384e5` `daf08db` `35c761a` `d405fd2` `f1d99ae` `d6f4275` `4ad9207` `b9580c5` `fc8b4c2` + this one

## Did

- Fast-forwarded `cursor/images` onto `main`, killed stale 8080, linked photos, staged the two reels, ran the green gates (wxr / typecheck / copy / redirects / build / tokens / `check:alt` at **0/887** / qa-render).
- Rewrote [`workorders/cursor-plan.md`](../cursor-plan.md) as this session’s living plan and committed it before any other work.
- Looked at the four squares, the unusable-at-full-bleed set, `heroFor()` vs a human on the 17 named slugs, and the ragged wall at 1440. Wrote [`content/findings/photography-render-finding.md`](../../content/findings/photography-render-finding.md). Did not restyle.
- Added a Cursor-lane addendum to [`content/findings/homepage-video-finding.md`](../../content/findings/homepage-video-finding.md): 390, 1440, the 45% tint, and the `-ss 4` poster. Did not salvage footage or change `bg-black/45`.
- Reconciled the four `median_w` rows. Not only an even-set convention — Free People and Hudson also disagree on **n** (featured-outside-gallery). Wrote [`content/findings/median-w-finding.md`](../../content/findings/median-w-finding.md). Restated C-2. Did not rewrite `image-audit.tsv` or patch `lib/projects.ts`.
- Started the 887-row alt pass in `content/images/alt-text.tsv` (one project, one commit, after looking at every frame). Stopped the remaining first-wave on Alexey’s request.

## Landed

| Path | What |
|---|---|
| `workorders/cursor-plan.md` | session 06 living plan (bar C) |
| `content/findings/photography-render-finding.md` | squares / unusable / heroFor 14/17 / ragged wall |
| `content/findings/homepage-video-finding.md` | 390 / 1440 / tint / poster addendum |
| `content/findings/median-w-finding.md` | four `median_w` rows explained |
| `content/images/alt-text.tsv` | 83 looked-at rows, 6 projects complete |

## Numbers

- `npm run check:alt` → `check-alt: 83/887 photographs have descriptive alt (804 to write) — 6/58 projects complete`
- Completed slugs (manifest counts): Mackage SoHo 11, Boqueria Restaurant 9, Kat & Theo 10, Lantern House 16, 795 5th ave The Pierre 16, 652 Hudson st 21.
- `heroFor()` matches a human on **14 of 17** named slugs (`content/findings/photography-render-finding.md`).
- Video tint computes `oklab(0 0 0 / 0.45)`. Poster `-ss 4` is a title card on both reels.
- Wall tiles at 1440: every tile **332px** wide, native hero aspect (Spring 332×85; squares 332×332).
- Green-gate start of session: `assertions failed: 0`; redirects `301=111 (legacy=37 cross=58 review=16) skipSlugs=10 reviewUnresolved=0 gone=2 live=58`; `check-retired-hex: ok`; `qa-render: all checks passed`.

## Decisions I made on my own

- Findings while the server was hot, then alt — the handoff’s alt-first order wastes a live 8080.
- One voice for alt. Did not invent a row to turn the gate green. Kat & Theo 1012 and Boqueria 850 carry `flag=people` because diners / cooks are in the frame.
- Stopped the remaining first-wave when asked. Did not write `40-e66th-st` (six frames had been opened; no rows written). Committed the already-looked-at 652 Hudson batch so 21 honest rows did not sit uncommitted.

## Proposed, not applied

- **Hero overrides** (no `heroOverride` column until Alexey wants it): Spring **1902** (`Spring-Studio.jpg` 1880×1058) instead of 1189; Free People **1022** (1695×1233) if the landing must be landscape; Kith portrait neon is a judgment, not a data fix.
- **Poster `-ss`:** pick a later timestamp only if these posters ever ship. Do not change `scripts/prepare-videos.mjs` for a review instrument.
- **C-2:** `lib/projects.ts` still reads audit `hero_capable` (34) and `featured_dims`. Adopt measured 35/58 and the manifest inside Phase F. Do not patch Grok-owned code from this lane.
- **Tint:** leave `bg-black/45`. 45% is right; do not change to 35 or 55.
- No PLAN §1 edit. No restyle of `HeroSlot` / Pattern C for the squares.

## Blocked

- Phase F still blocked on Eric + a Sanity project.
- Video **masters** (not these 1024×576 WhatsApp reels). Ship no video on this footage.
- Repo remote is still public.

## Skipped

- Rest of the first-wave alt (Alexey: skip for now). Next slug is `40-e66th-st` (19). Then `12th-st-townhouse-greenwich-village` (12), `e63-st-penthouse` (5), `lexington-ave-townhouse` (13), `west-23rd-townhouse-2` (10), `horatio-st-townhouse` (16), `free-people-retail-store` (7), `spring-studios-spring-place` (12).
- The other 44 live projects’ alt.
- 876 unreferenced library images; ten client PNGs in `Images_Streamline/`.
- Builder kill-list re-check of production.
- Sanity ingest / `_sanityAsset`.
- Implementing `heroOverride`, changing `bg-black/45`, restyling squares, patching `lib/projects.ts` 35/58.
- Rebuild-and-join of the last alt batch onto 8080 (stop was mid-wave).
- Push; commits on `main`; PLAN §1 edits.

## Next step

Continue alt at `40-e66th-st` (19 frames).
