# Handoff — Grok Phase E — 2026-08-20

Cursor: reproduce these claims. Do not take them on faith. Branch `grok/build` is not on origin (remote public — do not push). Pull it once Alexey flips the remote private, or copy the tree.

## Claims

| # | Claim | Command |
|---|---|---|
| 1 | Typecheck clean | `cd sl && npm run typecheck` |
| 2 | Kill-list clean | `npm run check:copy` |
| 3 | No retired hex in CSS | `npm run check:tokens` |
| 4 | Extractor assertions | `python3 scripts/wxr-extract.py` → `assertions failed: 0` |
| 5 | 58 live projects, 27C/31R | `/portfolio` tiles; `/commercial/<slug>` 404s a residential slug (e.g. `/commercial/lantern-house` → 404) |
| 6 | Direction C archive is 58 titles | `curl -s 'http://127.0.0.1:8080/?d=c'` contains 58 unique `href="/commercial|residential/..."` |
| 7 | Lightbox A collapsed, B expanded | `/commercial/kith?d=a` has `Show details`; `?d=b` has `Hide details` and `NoHo` / `7,500 sq ft` / `Snarkitecture` |
| 8 | Skipped fields do not render | `/commercial/bad-roman-columbus-circle-mall?d=b` has no `Designer ·`, no `12,500`, no `Michael Stillman`, no `Columbus Circle Mall` as a location line |
| 9 | Architect omitted when null | `/residential/lantern-house?d=b` has Chelsea + size, no `Architect ·`, no `None` |
| 10 | Nav variants | `/?d=a` primary nav is the DESIGN §5 set; `/?d=c` is Work / About us / Services / Contact / Request for pricing; `/?d=b&nav=partners` includes Partners |
| 11 | Testimonials | `/clients?t=quotes` → 8 `<blockquote>`, Mercer 0, `Antonio Di Oronzo` present; `/clients?t=reconsent` → 0 quotes |
| 12 | B proof points only on B | `/about?d=b` contains “millwork shop” and “7 working days”; `/about?d=a` contains neither |
| 13 | Three widths, no overflow | `NODE_PATH=/path/to/playwright/node_modules node scripts/qa-directions.mjs` — 8 routes × 390/768/1440, exit 0 |
| 14 | Lightbox keyboard (headless) | ArrowRight from `/commercial/kith?d=a` pages to the next commercial slug; Escape and × land on `/portfolio`; Tab cycles Previous / Next / Show details / Close |

## Verified here

- 1–12 against `npm run dev` HTML and the JSON/TSV sources.
- 13 as Playwright screenshots under `/workspace/screenshots/e-*-{390,768,1440}.png` (sandbox path, not committed).
- 14 as Playwright `keyboard.press`, not a physical keyboard.

## Unverified (your machine)

- Contrast of Pattern C columns, lightbox type on the dark field, and red-on-dark Review labels on a real screen.
- Physical keyboard and focus ring appearance.
- `prefers-reduced-motion` stills (no auto-rotate) on a real OS setting.
- `npm run build` — not run; dev is serving 8080 and a production build would replace `.next`.

## Skips

- Photography: empty `ProjectImageSlot` everywhere. Do not generate, stock, or trace.
- Video homepage variants: dark empty fields only (PLAN §1 row 32).
- Partner names/artwork: `content/copy/partners.json` names: [] → zero slots.
- Unresolved A-row fields listed in `lib/skipFields.ts`.
- Sqft bands: C sorts, it does not bucket.
- In-project image paging: chevrons page **projects** until your manifest lands.
- `deck-corrections.tsv` still missing; testimonial `decks` variant = the 8 published quotes.

## Files close to your boundary

None written under `content/images/**`. Consumed nothing of yours because nothing has landed. When the manifest arrives, `ProjectImageSlot` is the mount point; `HeroSlot` already pillarboxes the four `SQUARE_SLUGS` in direction A.

## How to switch directions

Public URLs do not change. For Eric:

- `/?d=a` Faithful · `/?d=b` Depth · `/?d=c` Archive
- `&home=still\|stills\|video-loop\|video-scroll`
- `&nav=reference\|partners\|split\|minimal`
- `&t=quotes\|reconsent\|decks\|logos`

Or the collapsed **Review** control at the bottom of the sidebar.
