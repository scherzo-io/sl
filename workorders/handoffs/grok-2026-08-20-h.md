# Handoff — Grok Phase H — 2026-08-20

Cursor: this is the honest gate table, not a victory lap. Reproduce anything in **Passed** that you can; the **Skipped** rows are yours or Eric's.

Lane boxes from `grok-build.md` §6 / `CLAUDE.md` verification gates.

| Gate | Result | Evidence |
|---|---|---|
| 58 projects (27C / 31R), 10 pages, 2 categories, 2 people **in Sanity** | **Skipped** | F not run. No Sanity project. Data layer (inventory + pages + people.json) has 58 / 10 / 2 / 2 |
| 887 image assets accounted for | **Skipped** | Cursor lane. `image-audit.tsv` still 887 referenced |
| 8 featured-outside-gallery resolve a real `heroImage` | **Skipped** | No photographs. `HeroSlot` empty |
| `roles[]` only the six atoms; `roleDetail` populated; no whitespace variants | **Passed at data layer** | All 58 `role_raw_live` map through `RAW_TO_ATOMS`. Unmapped 0. Whitespace-raw 0. Not in Sanity |
| `architect` null on exactly 8; credit line hidden | **Passed at data layer / E** | 5 `missing` + 3 `pseudo-blank` (`None` / `None Involved`). Credit hidden in `ProjectView` from E. Not re-clicked this session |
| `sizeSqFt` numeric on all 58 | **Partial** | Inventory `size_sqft_live` numeric on 58. Render skips 6 conflict slugs (`lib/skipFields.ts`). Sanity field not written (F) |
| No duplicate slugs; category/person refs resolve | **Passed at data layer** | 58 unique live slugs. People: eric-ortense, liam-treanor. No CMS refs |
| Output greps clean: `<p`, `_cdata`, `[object Object]`, `555-1234`, `456-7890`, `123 Construction`, `Hello world` | **Passed** | `npm run check:copy` clean. `desiogn` remains only in `flags.json` (the fix record), not in `check:copy` paths |
| Red `#DA2128` / `#E25257`; no retired reds | **Passed** | `npm run check:tokens` → 0 retired hexes |
| Three directions at 390 / 768 / 1440; lightbox keyboardable; `prefers-reduced-motion` | **Not re-run** | Claimed in E handoff. Screenshots not recaptured this session |
| Every legacy URL 200 / 301 / intentional 410, including 73 TSV rows, 11 SKIP still 200 | **Passed locally** | `npm run check:redirects` HTTP clean. Production HEAD: Cursor. 25 REVIEW are intentional 404, not 410 |
| `python3 scripts/wxr-extract.py` → `assertions failed: 0` | **Passed** | printed `assertions failed: 0` |
| `npm run dev` and `npm run build` clean; TypeGen current | **Partial** | `dev` serving 8080. `typecheck` clean. **`build` not run** (would clobber `.next`). TypeGen not re-run (schema description-only) |

## Proposed PLAN §1 rows (do not apply here)

1. Remote `scherzo-io/sl` is public. Neither lane pushes. Supersedes PLAN §10 “private”.
2. `upper-east-side-penthouse__trashed` is a WP artefact, never a public URL. Extractor recommends 410. Phase G left it 404 with REVIEW. Alexey can flip it to 410 without Eric.
3. Redirect documents live in CMS after F. Until then the Edge seed is `content/copy/redirects.json`. Adding a 301 does not currently require a Sanity publish.

## Proposed COWORK.md §6

`2026-08-20 | Grok G+H | 37×301, 11 SKIP 200, 25 REVIEW 404, 2×410; consent banner, IDs null; F skipped; do not push`

## What failed

Nothing in this lane's runnable set failed. The boxes that look empty are skipped with a reason, not silent.

## Files close to your boundary

Same as G. Image-count and live-HEAD boxes are yours.
