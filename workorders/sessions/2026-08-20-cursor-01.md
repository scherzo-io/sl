# cursor session 01 — 2026-08-20

**Phase:** bar B Wave 1 (measure / link-check / decks / brand); waiting on Grok handoff
**Branch:** cursor/images
**Commits:** `6a33330` (plan), `41633b1` (Wave 1 artifacts)

## Did

- Branched `cursor/images`. `python3 scripts/wxr-extract.py --uploads wp-content/uploads` → `assertions failed: 0`. Restored dirty `inputs/derived`.
- Committed `workorders/cursor-plan.md` before execution.
- Parallel seats: Grok 4.5 manifest/measure; Grok 4.6 live link-check; Composer decks/partners/brand/video-ask.
- Light GPT-5.6-sol reviews (3): manifest and link-check confirmed; partners README cited commercial p41 OCR — parent rewrote that line to workbook-only before commit.
- Confirmed dump has zero video files. Questions for Alexey recorded in `content/images/video-ask.md`.

## Landed

| Path | What |
|---|---|
| `workorders/cursor-plan.md` | executable bar-B plan |
| `scripts/build-image-manifest.mjs` | Node + sips; 887 measured |
| `content/images/image-manifest.tsv` | 887 rows, true originals |
| `content/images/checksums-887.tsv` | sha256 of chosen sources |
| `content/images/README.md` | regenerate + reviewer checks |
| `content/findings/hero-measure-finding.md` | PLAN hero 34 → measured 35 |
| `content/link-check/2026-08-20.tsv` | live sweep, dated |
| `content/deck-corrections.tsv` | ship-string OCR corrections |
| `content/images/partners/` | 30 names + ID crops |
| `content/images/brand/` | `#DE2426` sample + interim rasters |
| `content/images/video-ask.md` | zero videos + five questions |

## Numbers

- Referenced / larger original / parent_is_zero / featured_only: **887 / 388 / 137 / 7** (recomputed by the manifest script).
- Hero-capable projects: **35/58** (`measured_w >= 1920`). PLAN §7 still says 34. Proposed row below.
- Featured-outside-gallery sources on disk: **8/8**.
- Dump: `find wp-content/uploads -type f | wc -l` → 25883; `du -sk wp-content/uploads` → 12129628. Videos in dump: **0**.
- Link-check (measured-from-live 2026-08-20, sequential + 400ms): 135 unique GETs; 58 projects 200; all 11 SKIP live projects 200; `/sample-page/` and `/1248-2/` still 200; WP 564 still live at `/commercial/upper-east-side-townhouse/`; WP 558 still at `autrium-corporate-office`.
- Logo master modal red: **`#DE2426`** (reproduces remembered sample; Δ vs locked `#DA2128` is +4,+3,−2).

## Decisions I made on my own

- Bar B, no Sanity, no 887 alt-text this session.
- Hero rule = `measured_w >= 1920` on the chosen source, not WP served dims.
- Testimonials not visually verified (REFERENCES pages). Names from the workbook only.
- Partner ID-crops stay in-repo, labelled not-for-publication.

## Proposed, not applied

- PLAN §1 new row: hero-capable projects are **35 of 58**, superseding §7's 34. Evidence: `content/findings/hero-measure-finding.md` + `node scripts/build-image-manifest.mjs --uploads wp-content/uploads`.
- `#DA2128` stays the locked token; `#DE2426` is now a reproducible sample of the PNG master, not a remembered one.

## Blocked

- **Alexey — videos.** None on disk. Five questions in `content/images/video-ask.md`.
- **Eric — publishable partner marks.** Names only; crops are not artwork.
- **Grok handoff.** `workorders/handoffs/` empty aside from README. Review waits on it.
- **Sanity project.** Explicitly skipped this run.

## Skipped

- Sanity ingest / `_sanityAsset` / TypeGen
- 887 alt-text
- 876 unreferenced + `uploads/backup/` glance
- Ten client PNGs in `~/Downloads/.../Images_Streamline/`
- Builder kill-list re-verify
- Viewport / lightbox marathon (no Grok UI yet)
- Push

## Next step

When Grok writes `workorders/handoffs/grok-*.md`, review it into `workorders/reviews/` (report only), then hand the leftover list to Claude Code.
