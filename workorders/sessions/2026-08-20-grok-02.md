# grok session 02 — 2026-08-20

**Phase:** B (scaffold)
**Branch:** `grok/build`
**Commits:** this session (see git log on the branch)

## Did

- `git fetch`. `origin/main` still `347aea5`. No new remote commits since session 01.
- Committed Phase A (`ac74339`).
- Scaffolded Next.js App Router 15 + TypeScript + Tailwind v4 **inside `sl/`** (not the outer Vite sandbox). Tokens from `lib/tokens.ts` mirrored in `app/globals.css`.
- Shell: 200px sidebar from 1024px up; dark top bar + hamburger below. Patterns A / B / C as layout primitives and as `/`, `/portfolio`, `/services`.
- `/studio` reserved as a stub — no Sanity project id, so next-sanity is not mounted.
- Photography slots empty (`ProjectImageSlot`). Wordmark is type. Phone and social omitted (no sourced social; phone pending Eric).
- Gate commands: `npm run build` clean (after a clean `.next`), `npm run typecheck` clean, `node scripts/check-retired-hex.mjs` 0 hits. Pattern screenshots at 390 / 768 / 1440.
- Did not push (`scherzo-io/sl` is public).

## Landed

| Path | What |
|---|---|
| `app/**` `components/**` `lib/**` | shell, patterns A/B/C, sourced copy subset |
| `package.json` `next.config.mjs` `tsconfig.json` | Next 15 / React 19 / Tailwind 4 |
| `sanity/env.ts` | empty `projectId` guard |
| `scripts/check-retired-hex.mjs` | Phase B CSS gate |
| `public/favicon.svg` | red bar on sidebar ground — not a traced logo |
| `workorders/handoffs/grok-2026-08-20-b.md` | Phase B claims for Cursor |

## Numbers

- `python3 scripts/wxr-extract.py` → `assertions failed: 0` (session start)
- `npm run build` → 18 routes generated (10 site + 3 pattern inspection + studio + not-found + extras)
- Built CSS hexes: `#000` `#fff` `#1a1a1a` `#2d2d2d` `#da2128` `#e25257` — no retired set
- Portfolio wall: 58 tiles from `content/image-audit.tsv` (TOTALS row skipped), native `featured_dims`
- Type scale in CSS: 14.56 / 16 / 19.2 / 20.8; body 16px; `transition: all 0.8s`; `prefers-reduced-motion` kills it

## Decisions I made on my own

- Studio is a reserved stub, not a mounted `NextStudio`, because `PROJECT_ID` is empty. Schema is Phase C.
- Sidebar breakpoint is `lg` (1024px). 768 uses the mobile top bar (200px sidebar + 400px column does not fit).
- About us is a click disclosure, collapsed by default — hover-open plus the global 800ms `max-height` transition left the submenu half-open in screenshots.
- Pattern C pages use a sourced copy *subset*: about mission + homepage story; six service blurbs; property-management lead; contact address + email. Dropped: about.txt lorem, Procore sentence, phones, partner bios (revenue/staffing/old address), Mercer testimonial, RFP form fields.
- Wordmark lettering is “Streamline”, not a copy of `inputs/raw/brand/streamline-logo.png`.
- `qa-patterns.mjs` lives in this repo for Cursor to reproduce 390/768/1440; it imports Playwright from the machine, not from `package.json`.

## Proposed, not applied

- PLAN §1: remote is currently public; neither lane pushes. Same proposal as session 01.
- COWORK.md §6: `2026-08-20 | Grok session 02 | Phase B Next.js scaffold on grok/build; tokens + patterns A/B/C; studio stub; not pushed`

## Blocked

- Sanity project + write token: Alexey (schema deploy / Studio mount)
- Phone numbers: Eric (sidebar and contact omit them)
- Vector logo: Eric (type wordmark until then)
- Photographs: Cursor image lane
- Push: Alexey, once the remote is private

## Skipped

- Phase C schema / TypeGen — next on this branch
- Directions A/B/C (Phase E) and lightbox
- Testimonials body, partner bios, RFP form
- `next-sanity` install
- OG / JSON-LD / redirects (Phase G)
- Production `npm run preview` on a second port — not run in the same `.next` as `next dev` (wiping `.next` while dev was up 500'd the preview; rebuilt clean, then restarted dev only)
- Push

## Next step

Phase C — Sanity schema (11 documents + `seo` + `blockContent`), TypeGen committed, no schema deploy.
