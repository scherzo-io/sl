# grok session 05 — 2026-08-20

**Phase:** E (three directions)
**Branch:** `grok/build`
**Commits:** this session

## Did

- Implemented all three directions fully, switchable, with public URLs unchanged (`/`, `/portfolio`, `/commercial/<slug>/`, `/residential/<slug>/`).
- Review state: cookie `sl-review` + query `d` / `home` / `nav` / `t`. Collapsed **Review** control in the sidebar, not in primary nav.
- Homepage variants: still · stills (34 hero-capable empty slots, no auto-rotate under `prefers-reduced-motion`) · video-loop and video-scroll declared-but-empty (PLAN §1 row 32). Direction C home is the typographic archive of 58 titles.
- Project lightbox at `/commercial/<slug>` and `/residential/<slug>`: A collapsed until expand; B expanded metadata; C editorial case study + empty sequence of `imageCount-1` slots. Esc / arrows / focus trap.
- Portfolio wall tiles link to project URLs. B two-level filter (category + the 3 assigned sub-categories). C adds trade atoms + sqft sort (no invented bands).
- Partners wall mounted at `/partners` with zero slots (`partners.json` names: []).
- Unresolved conflict fields skipped per `lib/skipFields.ts`. Architect line omitted when null (8 of 58).

## Landed

| Path | What |
|---|---|
| `lib/review.ts` `lib/skipFields.ts` `lib/projectTypes.ts` | review state, skip map, serializable project types |
| `lib/nav.ts` `lib/projects.ts` | `navFor` / `projectIndexHref`; live 58 with skips + entity decode |
| `middleware.ts` | query → cookie + `x-sl-review` for SSR |
| `components/review/**` | provider, collapsed Review, B-only proof points |
| `components/media/{HomeHero,ArchiveIndex,ProjectView,HeroSlot,PartnersWall}.tsx` | directions |
| `components/patterns/{PortfolioWall,ProjectFilters}.tsx` | linked wall + B/C filters |
| `app/commercial/[slug]` `app/residential/[slug]` `app/partners` | new routes |
| `scripts/qa-directions.mjs` | 390 / 768 / 1440 screenshots |

## Numbers

- `python3 scripts/wxr-extract.py` → `assertions failed: 0`
- Live projects loaded: 58 (27 commercial / 31 residential). Pipeline 8 excluded.
- Hero-capable stills: 34 (`image-audit.tsv` YES).
- Architect null: 8 (5 missing + 3 pseudo-blank).
- Sub-category assigned: 3 (Nightlife & Events, Restaurants & Bars, Retail & Showrooms).
- Square 2048×2048: 4 (`SQUARE_SLUGS`). Skip-field slugs: 12.
- `/?d=c` SSR: 58 unique project links.
- `/clients?t=quotes`: 8 blockquotes, Mercer 0. `/clients?t=reconsent`: 0 quotes.
- `npm run typecheck` clean. `npm run check:copy` clean. `npm run check:tokens` 0 retired hexes.
- `scripts/qa-directions.mjs`: 8 routes × 3 widths, no horizontal overflow, HTTP 200.
- Playwright (headless): ArrowRight pages Kith → next commercial; Escape and × both go to `/portfolio`; Tab cycles Previous / Next / Show details / Close.

## Decisions I made on my own

- Review cookie `sl-review` compact `a.still.reference.quotes`; query keys `d/home/nav/t`. Does not change public pathnames (PLAN §1 row 3).
- Direction C ignores nav/home variants and always uses the ARCHIVE nav (Work first, services demoted).
- Sub-category chips render only the 3 assigned values, not the empty 7 of the pending taxonomy.
- C “sqft filter” is sort (listed / smallest / largest), not invented bands.
- Testimonial `decks` = the same 8 published quotes (Cursor has not filed `deck-corrections.tsv`; do not add OCR). `reconsent` renders the title and no quotes. `logos` is the empty partners wall.
- Decode WP `&#038;` in titles at load (`Kat & Theo`, `Burger & Lobster`). Encoding, not a spelling change.
- Sibling catalog sent to the client is `{slug,category,title}` only, so skipped fields on other projects are not in the RSC payload.
- A pillarboxes the four square projects; B/C fill the slot. Photography is still empty.

## Proposed, not applied

- None. Public-remote / do-not-push row from session 01 still stands.

## Blocked

- Partner names and artwork: Cursor
- Video files: Alexey (row 32) — two homepage variants stay empty
- Phones, Procore, Mercer, pipeline copy, estimating address, sub-category backfill: Eric
- Sanity project: Alexey (schema exists, not deployed)
- `npm run build` not run this session — dev server is on 8080; production build would clobber `.next`

## Skipped

- In-project gallery paging (no photographs — chevrons page projects)
- Invented sqft bands
- Publishing unresolved skip fields
- Partner OCR names
- Stock or generated photography
- Phase F migration, G redirects/SEO, H verify

## Next step

Phase F is blocked on Eric’s answers + a Sanity project + Cursor images. Do not start F. Cursor reviews this handoff on a real screen and keyboard.
