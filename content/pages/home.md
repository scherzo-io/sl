# Home

- live: `/` (WordPress slug `template`)
- pattern: A (landing, non-scrolling)
- copy source: `inputs/derived/pages/template.txt`

The live homepage is a long Elementor story + services teaser + contact stack. The rebuilt landing is Pattern A — one photograph, no scroll — so that copy is **not laid out here**. It is assembled on `/about/` (`content/copy/about.json` story) and `/services/`.

Contact facts on the live home (email, address) render from `content/copy/site-settings.json`. Phones omitted.

Photography: empty slot until Cursor's manifest.
